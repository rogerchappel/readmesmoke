# Example project

This tiny project gives readmesmoke a deterministic fixture to run.

```bash
echo readmesmoke fixture
```

```bash
node hello.js
```

This command is intentionally unsafe and should be skipped unless a maintainer changes policy:

```bash
sudo echo no
```
