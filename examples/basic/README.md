# Example project

This tiny project gives readmesmoke a deterministic fixture to run. Commands
run from this README's directory in the temporary workspace, so `hello.js` is
resolved as its sibling.

```bash
echo readmesmoke fixture
```

```bash
node hello.js
```
