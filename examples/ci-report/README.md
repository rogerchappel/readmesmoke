# CI report example

This fixture models a README with a tiny verification command and a command
that should stay in planning output until a maintainer explicitly allowlists it.

```sh
echo docs are runnable
```

```sh
npm publish
```
